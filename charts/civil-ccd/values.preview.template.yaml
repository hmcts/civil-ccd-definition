civil-service:
  java:
    keyVaults:
      civil:
        resourceGroup: civil
        secrets:
          - civil-idam-client-secret
          - microservicekey-civil-service
          - system-update-user-username
          - system-update-user-password
          - cross-access-user-username
          - cross-access-user-password
          - prd-admin-user-username
          - prd-admin-user-password
          - AppInsightsInstrumentationKey
          - docmosis-api-key
          - gov-notify-api-key
          - sendgrid-api-key
          - robotics-notification-sender
          - robotics-notification-recipient
          - robotics-notification-recipient-spec
          - name: launch-darkly-sdk-key-non-prod
            alias: launch-darkly-sdk-key
          - ordnance-survey-api-key
          - robotics-notification-multipartyrecipient
          - notification-caseworker-spec-receiver
    environment:
      LAUNCH_DARKLY_ENV: ${SERVICE_FQDN}
      OIDC_ISSUER: https://forgerock-am.service.core-compute-idam-aat2.internal:8443/openam/oauth2/realms/root/realms/hmcts
idam-pr:
  redirect_uris:
    XUI:
      - https://xui-${SERVICE_FQDN}/oauth2/callback

civil-general-applications:
  enabled: true
xui-webapp:
  nodejs:
    ingressHost: xui-${SERVICE_FQDN}
ccd:
  ccd-api-gateway-web:
    nodejs:
      ingressHost: gateway-${SERVICE_FQDN}
  ccd-data-store-api:
    java:
      environment:
        OIDC_ISSUER: https://forgerock-am.service.core-compute-idam-{{ .Values.global.environment }}.internal:8443/openam/oauth2/hmcts
        CCD_S2S_AUTHORISED_SERVICES_CASE_USER_ROLES: civil_service,aac_manage_case_assignment
      ingressHost: ccd-data-store-api-${SERVICE_FQDN}
  ccd-definition-store-api:
    java:
      ingressHost: ccd-definition-store-${SERVICE_FQDN}
  am-role-assignment-service:
    java:
      ingressHost: am-role-assignment-service-${SERVICE_FQDN}
camunda-bpm:
  java:
    ingressHost: camunda-${SERVICE_FQDN}

wa:
  enabled: true


wa-task-management-api:
  enabled: true
  java:
    releaseNameOverride: ${SERVICE_NAME}-wa-task-management-api
    ingressHost: wa-task-management-api-${SERVICE_FQDN}
    environment:
      CAMUNDA_URL: http://camunda-${SERVICE_FQDN}/engine-rest
      ROLE_ASSIGNMENT_URL: http://am-role-assignment-service-${SERVICE_FQDN}
      POSTGRES_HOST: "{{ .Release.Name }}-postgresql"
      POSTGRES_NAME: "{{ .Values.postgresql.auth.database}}"
      POSTGRES_USERNAME: "{{ .Values.postgresql.auth.username}}"
      POSTGRES_PASSWORD: "{{ .Values.postgresql.auth.password}}"
      POSTGRES_CONNECTION_OPTIONS: ""

wa-task-monitor:
  enabled: true
  java:
    ingressHost: wa-task-monitor-${SERVICE_FQDN}
    releaseNameOverride: ${SERVICE_NAME}-wa-task-monitor
    environment:
      CAMUNDA_URL: http://camunda-${SERVICE_FQDN}/engine-rest
      TASK_MANAGEMENT_SERVICE_URL: http://wa-task-management-api-${SERVICE_FQDN}
      CASE_EVENT_HANDLER_SERVICE_URL: http://wa-case-event-handler-${SERVICE_FQDN}
      CCD_SEARCH_URL: http://ccd-data-store-api-${SERVICE_FQDN}
      CCD_URL: http://ccd-data-store-api-${SERVICE_FQDN}
      ROLE_ASSIGNMENT_URL: http://am-role-assignment-service-${SERVICE_FQDN}

wa-case-event-handler:
  enabled: true
  java:
    ingressHost: wa-case-event-handler-${SERVICE_FQDN}
    releaseNameOverride: ${SERVICE_NAME}-wa-case-event-handler
    secrets:
      AZURE_SERVICE_BUS_CONNECTION_STRING:
        secretRef: servicebus-secret-topic-{{ .Release.Name }}-servicebus-ccd-case-events # todo is this right?
        key: connectionString
    environment:
      CAMUNDA_URL: http://camunda-${SERVICE_FQDN}/engine-rest
      CCD_URL: http://ccd-data-store-api-${SERVICE_FQDN}
      ROLE_ASSIGNMENT_URL: http://am-role-assignment-service-${SERVICE_FQDN}
      WA_WORKFLOW_API_URL: http://wa-workflow-api-${SERVICE_FQDN}
      WA_TASK_MANAGEMENT_API_URL: http://wa-task-management-api-${SERVICE_FQDN}
      AZURE_SERVICE_BUS_TOPIC_NAME: ccd-case-events
      AZURE_SERVICE_BUS_SUBSCRIPTION_NAME: wa-case-events-sub
      AZURE_SERVICE_BUS_CCD_CASE_EVENTS_SUBSCRIPTION_NAME: wa-ccd-case-events-sub
      POSTGRES_HOST: "{{ .Release.Name }}-postgresql"
      POSTGRES_NAME: "{{ .Values.postgresql.auth.database}}"
      POSTGRES_USERNAME: "{{ .Values.postgresql.auth.username}}"
      POSTGRES_PASSWORD: "{{ .Values.postgresql.auth.password}}"
      POSTGRES_CONNECTION_OPTIONS: ""

wa-workflow-api:
  enabled: true
  java:
    ingressHost: wa-workflow-api-${SERVICE_FQDN}
    releaseNameOverride: ${SERVICE_NAME}-wa-workflow-api
    environment:
      CAMUNDA_URL: http://camunda-${SERVICE_FQDN}/engine-rest
      POSTGRES_HOST: "{{ .Release.Name }}-postgresql"
      POSTGRES_NAME: "{{ .Values.postgresql.auth.database}}"
      POSTGRES_USERNAME: "{{ .Values.postgresql.auth.username}}"
      POSTGRES_PASSWORD: "{{ .Values.postgresql.auth.password}}"
      POSTGRES_CONNECTION_OPTIONS: ""
      TASK_MANAGEMENT_URL: http://wa-task-management-api-${SERVICE_FQDN}

wa-task-batch-service:
  java:
    environment:
      WA_TASK_MONITOR_SERVICE_URL: http://wa-task-management-api-${SERVICE_FQDN}

wa-message-cron-service:
  releaseNameOverride: ${SERVICE_NAME}-ccd-message-publisher
  java:
    environment:
      WA_CASE_EVENT_HANDLER_SERVICE_URL: http://wa-case-event-handler-${SERVICE_FQDN}

ccd-message-publisher:
  enabled: true
  java:
    ingressHost: ccd-message-publisher-${SERVICE_FQDN}
    releaseNameOverride: ${SERVICE_NAME}-ccd-message-publisher
    environment:
      DATA_STORE_DB_HOST: "{{ .Release.Name }}-postgresql"
      DATA_STORE_DB_NAME: "{{ .Values.postgresql.auth.database }}"
      DATA_STORE_DB_USERNAME: "{{ .Values.postgresql.auth.username }}"
      DATA_STORE_DB_PASSWORD: "{{ .Values.postgresql.auth.password }}"
      DATA_STORE_DB_OPTIONS: "?stringtype=unspecified"
      CCD_CASE_EVENTS_DESTINATION: ccd-case-events
  secrets:
    SERVICE_BUS_CONNECTION_STRING:
      secretRef: servicebus-secret-topic-{{ .Release.Name }}-servicebus-ccd-case-events
      key: connectionString

servicebus:
  enabled: true
  teamName: CCD # todo Civil?
  resourceGroup: ccd-aks # todo Civil?
  serviceplan: standard
  setup:
    topics:
      - name: ccd-case-events
        subscriptionNeeded: yes
      - name: wa-case-events-sub
        subscriptionNeeded: yes
      - name: wa-ccd-case-events-sub
        subscriptionNeeded: yes
