const obligationDate = {
  ObligationDate: {
    obligationDatePresent: 'No',
  },
};

const isFinalOrder = {
  IsFinalOrder: {
    isFinalOrder: 'Yes',
  },
};

const nextSteps = {
  NextSteps: {
    courtStaffNextSteps: 'NO_TASKS',
  },
};

const confirmOrderReviewDataBuilderComponents = {
  obligationDate,
  isFinalOrder,
  nextSteps,
};

export default confirmOrderReviewDataBuilderComponents;
