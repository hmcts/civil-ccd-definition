export const subHeadings = {
  uploadClaimTimelineTemplate: 'Upload Claim timeline template',
};

export const labels = {
  uploadFiles: 'Upload files',
};

export const hints = {
  uploadFileHint:
    'We only accept documents in pdf format. Please do not upload password protected documents as this will prevent the claim from being processed.',
};

export const uploadDocument = {
  specClaimTemplateDocumentFiles: '#specClaimTemplateDocumentFiles',
};

export const buttons = {
  cancelUpload: 'Cancel upload',
};
