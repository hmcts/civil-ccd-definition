export const heading = (defendantName: string) => [`Why does ${defendantName} dispute the claim?`];

export const input = {
  selector: '#detailsOfWhyDoesYouDisputeTheClaim',
};

export const subheading = 'Upload supporting evidence (optional)';
